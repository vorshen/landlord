import Button, { i_ButtonOptions } from "../../button/button";

class BackButton extends Button {
    constructor(options: i_ButtonOptions) {
        super(options);
    }
};

export default BackButton;